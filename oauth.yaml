apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
  - htpasswd:
      fileData:
        name: localuser
    mappingMethod: claim
    name: localusers
    type: HTPasswd
