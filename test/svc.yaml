apiVersion: v1
kind: Service
metadata:
  labels:
    app: salesforce-webhook-ingestor
  name: salesforce-webhook-ingestor
  namespace: integration
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: salesforce-webhook-ingestor
